# Transaction Categoriser (Baseline)

## Overview
This repo implements a TF-IDF + LogisticRegression baseline for automated transaction categorization. It includes synthetic data generation, preprocessing, training, prediction, and evaluation scripts.

## Setup
1. Create virtualenv & install:
```bash
python -m venv .venv
source .venv/bin/activate   # Windows: .venv\Scripts\activate
pip install -r requirements.txt
